{
  "name": "morechard-worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --remote",
    "deploy": "wrangler deploy --env production",
    "deploy:preview": "wrangler versions upload --env production",
    "deploy:promote": "wrangler versions deploy --env production",
    "sync:playbook": "node scripts/sync-playbook.mjs",
    "sync:playbook:prod": "node scripts/sync-playbook.mjs --env production",
    "test": "vitest",
    "load-test": "node scripts/load-test.mjs",
    "load-test:auth": "node scripts/load-test-authenticated.mjs"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "@types/qrcode-svg": "^1.1.5",
    "autocannon": "^8.0.0",
    "typescript": "^5.5.2",
    "vitest": "^4.1.5",
    "wrangler": "^4.79.0"
  },
  "dependencies": {
    "@cloudflare/puppeteer": "^1.1.0",
    "@sentry/cloudflare": "^10.67.0",
    "@simplewebauthn/server": "^13.3.2",
    "qrcode-svg": "^1.1.0",
    "zod": "^4.4.3"
  }
}
